#include <stdio.h>
#include <conio.h>
void main()
{
int i,j;
char str1[5],str2[5];
printf("enter the string");
gets(str1);
gets(str2);
for(i=0;str1[i]!='\0';i++)
{
str1[i]==str2[i];
}
puts(str1);
}
